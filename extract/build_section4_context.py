"""
extract/build_section4_context.py

섹션4는 다른 섹션들과 성격이 다릅니다 — AI가 섹션4를 "직접 작성"하는 게
아니라, 지난 주차들에 작성됐던 섹션4 내용들을 요약해서 "작성자가 참고할
자료"로 제공하는 용도로 계획되어 있습니다. (작성 자체는 계속 사람이 담당)

※ 아직 구현되지 않았습니다. 자리만 마련해둔 상태입니다. 세부 계획 미정.

구현 전 확정되어야 할 것들 (착수 시 다시 논의 필요):
    - 과거 섹션4 텍스트를 어디서 가져올지
        → 아직 "완성된 보고서" 테이블 자체가 설계 전이라, 이 테이블이 먼저
          만들어져야 여기서 조회할 데이터가 생김 (storage 관련 미해결 의존성)
    - 몇 주치를 참고할지 (예: 최근 4주?)
    - 같은 계정(ad_account_id) 기준으로만 모을지, 아니면 전체 계정을 봐서
      맥락상 비슷한 것만 고를지 (RAG 아이디어와 연결 가능 — reference_notes.md
      섹션4 RAG 아이디어 참고)
    - "요약"의 형태 — 단순 나열인지, AI로 요약한 한두 문단인지

입력이 될 것으로 예상되는 것: 완성된 보고서 테이블의 section4_text 컬럼
(아직 미설계) + ad_account_id + 최근 N주
"""


def get_past_section4_texts(ad_account_id: int, weeks_back: int = 4) -> list:
    """
    지정 계정의 최근 N주치 섹션4 텍스트를 가져옵니다.

    Args:
        ad_account_id: 대상 광고 계정 ID
        weeks_back: 몇 주치를 가져올지 (기본 4주, 확정된 값 아님)

    Returns:
        list: 과거 섹션4 텍스트 목록

    Raises:
        NotImplementedError: 완성된 보고서 테이블이 아직 설계·생성되지 않았습니다.
    """
    raise NotImplementedError(
        "섹션4 참고자료 기능은 아직 구현되지 않았습니다. "
        "완성된 보고서 테이블(섹션4 텍스트 저장용)이 먼저 설계·생성되어야 합니다."
    )
